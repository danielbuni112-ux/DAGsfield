# Plan: Enhance Director App with New Theme and Layout

## Objective
Replace the current Director page layout with the provided redesigned theme, removing the unwanted left sidebar while preserving all existing functionality.

## Analysis
- Current DirectorPage.js is a vanilla JS component with DOM manipulation
- Provided code is a React component with a new dark theme and layout
- Need to convert React JSX to vanilla JS and integrate with existing logic
- Remove the narrow left sidebar (46px column with app icons) as it's already present in the application

## Tasks

### 1. Code Conversion and Layout Updates
- Convert React component to vanilla JS DOM creation
- Remove the first aside element (narrow sidebar with app icons)
- Adjust CSS grid from '46px 194px minmax(0,1fr) 214px' to '194px minmax(0,1fr) 214px'
- Merge the existing header (back button, title, reasoning engine) with the new design

### 2. Styling Integration
- Apply the new dark theme with gradients and shadows
- Maintain existing functionality while updating visual design
- Ensure responsive design and proper spacing

### 3. Functionality Preservation
- Keep all existing event handlers and logic
- Integrate agent buttons, chat functionality, quick actions
- Preserve processing status, timeline preview, export options
- Maintain video playback and URL parameter handling

### 4. Content Integration
- Use the new layout structure for agents panel, main content, and right sidebar
- Update agent grid with the new card design and gradients
- Implement the new chat interface with quick action buttons
- Add timeline preview and workflow status sections

### 5. Testing and Validation
- Verify all buttons and interactions work
- Test video loading and processing flows
- Ensure responsive behavior on different screen sizes
- Validate chat functionality and agent activation

## Implementation Order
1. Create new DOM structure based on provided layout
2. Remove unwanted sidebar and adjust grid
3. Integrate existing JavaScript logic
4. Apply new CSS styling
5. Test functionality end-to-end

## Questions for User
- Should the top navigation bar from the provided code be included, or stick with the current header?
- Any specific styling adjustments needed for the new theme?
- Should any additional features from the provided code be added beyond the layout?